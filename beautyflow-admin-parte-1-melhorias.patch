diff --git a/src/static/admin/css/admin.css b/src/static/admin/css/admin.css
index 38d9196..377ef4f 100644
--- a/src/static/admin/css/admin.css
+++ b/src/static/admin/css/admin.css
@@ -1,3 +1,4 @@
+/* 1. Design tokens */
 :root {
     --bg: #f6f7fb;
     --surface: #ffffff;
@@ -17,14 +18,32 @@
     --success-text: #166534;
     --error-bg: #fee2e2;
     --error-text: #991b1b;
+    --sidebar-width: 260px;
+    --content-max-width: 1440px;
+    --space-xs: 6px;
+    --space-sm: 10px;
+    --space-md: 16px;
+    --space-lg: 24px;
+    --space-xl: 32px;
     --radius: 16px;
-    --shadow: 0 12px 40px rgba(15, 23, 42, .08);
+    --radius-sm: 10px;
+    --radius-md: 14px;
+    --radius-lg: 20px;
+    --shadow: 0 12px 32px rgba(15, 23, 42, .07);
+    --shadow-soft: 0 8px 22px rgba(15, 23, 42, .05);
+    --focus-ring: 0 0 0 3px rgba(17, 24, 39, .14);
+    --transition: 160ms ease;
 }
 
+/* 2. Reset/base */
 * {
     box-sizing: border-box;
 }
 
+html {
+    min-height: 100%;
+}
+
 [hidden] {
     display: none !important;
 }
@@ -32,9 +51,12 @@
 body {
     min-height: 100vh;
     margin: 0;
-    font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
+    overflow-x: hidden;
     background: var(--bg);
     color: var(--text);
+    font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
+    line-height: 1.5;
+    -webkit-font-smoothing: antialiased;
 }
 
 a {
@@ -42,6 +64,11 @@ a {
     text-decoration: none;
 }
 
+img,
+svg {
+    max-width: 100%;
+}
+
 input,
 select,
 textarea,
@@ -52,47 +79,64 @@ button {
 input,
 select,
 textarea {
+    min-width: 0;
+}
+
+:focus-visible {
+    outline: none;
+    box-shadow: var(--focus-ring);
+}
+
+/* 3. Layout principal */
+.main {
     width: 100%;
-    border: 1px solid var(--border);
-    border-radius: 12px;
-    padding: 10px 12px;
-    background: #fff;
-    color: var(--text);
+    min-width: 0;
+    padding: var(--space-xl);
 }
 
-input[readonly],
-textarea[readonly],
-select:disabled {
-    background: var(--surface-2);
-    color: var(--muted);
-    cursor: default;
-    opacity: 1;
+.with-sidebar {
+    min-height: 100vh;
+    margin-left: var(--sidebar-width);
 }
 
-textarea {
-    resize: vertical;
+.with-sidebar > :not(.flash) {
+    max-width: var(--content-max-width);
 }
 
-label {
+.auth-main {
+    min-height: 100vh;
     display: grid;
-    gap: 6px;
-    font-weight: 600;
-    font-size: .92rem;
+    place-items: center;
 }
 
+.auth-card {
+    width: min(420px, calc(100vw - 32px));
+    padding: var(--space-xl);
+    border: 1px solid var(--border);
+    border-radius: var(--radius-lg);
+    background: var(--surface);
+    box-shadow: var(--shadow);
+}
+
+/* 4. Sidebar */
 .sidebar {
     position: fixed;
     inset: 0 auto 0 0;
-    width: 260px;
-    background: #0f172a;
-    color: white;
-    padding: 24px 18px;
+    width: var(--sidebar-width);
+    overflow-y: auto;
     display: flex;
     flex-direction: column;
-    gap: 24px;
+    gap: var(--space-lg);
+    padding: var(--space-lg) 18px;
+    border-right: 1px solid rgba(255, 255, 255, .08);
+    background: #0f172a;
+    color: white;
 }
 
 .brand {
+    display: inline-flex;
+    align-items: center;
+    min-height: 36px;
     font-size: 1.35rem;
     font-weight: 800;
     letter-spacing: -.03em;
@@ -100,13 +144,20 @@ label {
 
 .nav {
     display: grid;
-    gap: 6px;
+    gap: var(--space-xs);
 }
 
 .nav-link {
     padding: 11px 12px;
     border-radius: 12px;
     color: #cbd5e1;
+    font-weight: 700;
+    line-height: 1.35;
+    transition: background var(--transition), color var(--transition), transform var(--transition);
+}
+
+.nav-link:hover {
+    transform: translateX(2px);
 }
 
 .nav-link:hover,
@@ -116,16 +167,16 @@ label {
 }
 
 .sidebar-footer {
-    margin-top: auto;
     display: grid;
-    gap: 12px;
+    gap: var(--space-sm);
+    margin-top: auto;
 }
 
 .sidebar-user {
     min-width: 0;
     padding: 12px;
     border: 1px solid rgba(255, 255, 255, .12);
-    border-radius: 14px;
+    border-radius: var(--radius-md);
     background: rgba(255, 255, 255, .05);
 }
 
@@ -155,35 +206,17 @@ label {
     margin: 0;
 }
 
-.main {
-    padding: 32px;
-}
-
-.with-sidebar {
-    margin-left: 260px;
-}
-
-.auth-main {
-    min-height: 100vh;
-    display: grid;
-    place-items: center;
-}
-
-.auth-card {
-    width: min(420px, calc(100vw - 32px));
-    background: var(--surface);
-    border: 1px solid var(--border);
-    border-radius: 24px;
-    padding: 32px;
-    box-shadow: var(--shadow);
-}
-
+/* 5. Tipografia e headers */
 .page-header {
     display: flex;
     justify-content: space-between;
     align-items: flex-start;
-    gap: 16px;
-    margin-bottom: 24px;
+    gap: var(--space-md);
+    margin-bottom: var(--space-lg);
+}
+
+.page-header h1 {
+    line-height: 1.15;
 }
 
 h1,
@@ -193,109 +226,221 @@ h2 {
 }
 
 h1 {
-    font-size: 2rem;
+    font-size: clamp(1.65rem, 2vw, 2rem);
+    font-weight: 800;
 }
 
 h2 {
     font-size: 1.15rem;
+    font-weight: 750;
+    line-height: 1.25;
 }
 
 .muted {
+    max-width: 720px;
+    margin-top: var(--space-xs);
     color: var(--muted);
-    margin-top: 6px;
+    line-height: 1.5;
 }
 
-.notice {
-    background: #eef2ff;
-    border: 1px solid #c7d2fe;
-    padding: 12px 14px;
-    border-radius: 14px;
+.section-title {
+    margin-bottom: 4px;
 }
 
+.card-header {
+    display: flex;
+    justify-content: space-between;
+    align-items: flex-start;
+    gap: var(--space-md);
+    margin-bottom: var(--space-md);
+    padding-bottom: var(--space-sm);
+    border-bottom: 1px solid var(--border);
+}
+
+/* 6. Cards e grids */
 .card,
 .mini-card {
-    background: var(--surface);
+    margin-bottom: var(--space-lg);
+    padding: var(--space-lg);
     border: 1px solid var(--border);
     border-radius: var(--radius);
-    padding: 20px;
+    background: var(--surface);
     box-shadow: var(--shadow);
-    margin-bottom: 20px;
-}
-
-.integrations-card .cards-grid > .mini-card {
-    margin-bottom: 0;
 }
 
-.card-header {
-    display: flex;
-    justify-content: space-between;
-    align-items: center;
-    gap: 12px;
-    margin-bottom: 16px;
-}
-
-.stack {
-    display: grid;
-    gap: 14px;
-    align-content: start;
+.card,
+.mini-card,
+.stat-card {
+    min-width: 0;
 }
 
-.admin-search-card {
-    align-self: start;
-    height: fit-content;
+.card > h2:not(:last-child),
+.card > .section-title:not(:last-child) {
+    margin-bottom: var(--space-md);
 }
 
-.admin-search-card .button {
-    margin-top: 2px;
+.integrations-card .cards-grid > .mini-card {
+    margin-bottom: 0;
 }
 
-.form-grid {
+.stack {
     display: grid;
-    grid-template-columns: repeat(4, minmax(0, 1fr));
-    gap: 14px;
-    align-items: end;
+    align-content: start;
+    gap: var(--space-md);
 }
 
 .grid.two-columns {
     display: grid;
-    grid-template-columns: 1fr 1fr;
-    gap: 20px;
+    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
+    gap: var(--space-lg);
+    align-items: start;
 }
 
 .cards-grid {
     display: grid;
     grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
-    gap: 16px;
+    gap: var(--space-md);
 }
 
 .stats-grid {
     display: grid;
     grid-template-columns: repeat(4, minmax(0, 1fr));
-    gap: 16px;
-    margin-bottom: 20px;
+    align-items: stretch;
+    gap: var(--space-md);
+    margin-bottom: var(--space-lg);
 }
 
 .stat-card {
-    background: var(--surface);
+    display: grid;
+    align-content: start;
+    gap: 8px;
+    min-height: 128px;
+    padding: var(--space-lg);
     border: 1px solid var(--border);
     border-radius: var(--radius);
-    padding: 20px;
+    background: var(--surface);
+    box-shadow: var(--shadow-soft);
+    transition: transform var(--transition), box-shadow var(--transition);
+}
+
+.stat-card:hover {
+    transform: translateY(-1px);
     box-shadow: var(--shadow);
 }
 
 .stat-card strong {
     display: block;
     font-size: 2rem;
+    line-height: 1;
 }
 
 .stat-card span {
     color: var(--muted);
+    line-height: 1.4;
+}
+
+.admin-search-card {
+    align-self: start;
+    height: fit-content;
+}
+
+.admin-search-card .button {
+    margin-top: 2px;
 }
 
 .align-end {
     align-self: end;
 }
 
+.narrow-card {
+    max-width: 560px;
+}
+
+/* 7. Formulários */
+input,
+select,
+textarea {
+    width: 100%;
+    min-height: 42px;
+    padding: 10px 12px;
+    border: 1px solid var(--border);
+    border-radius: 12px;
+    background: #fff;
+    color: var(--text);
+    transition: border-color var(--transition), box-shadow var(--transition), background var(--transition);
+}
+
+input::placeholder,
+textarea::placeholder {
+    color: #9ca3af;
+}
+
+input:focus,
+select:focus,
+textarea:focus {
+    border-color: var(--primary-hover);
+    box-shadow: var(--focus-ring);
+    outline: none;
+}
+
+input[readonly],
+textarea[readonly],
+select:disabled {
+    background: var(--surface-2);
+    color: var(--muted);
+    cursor: default;
+    opacity: 1;
+}
+
+input[type="checkbox"],
+input[type="radio"] {
+    width: auto;
+    min-height: auto;
+    accent-color: var(--primary);
+}
+
+textarea {
+    resize: vertical;
+}
+
+label {
+    display: grid;
+    gap: var(--space-xs);
+    font-size: .92rem;
+    font-weight: 650;
+    line-height: 1.35;
+}
+
+.form-grid {
+    display: grid;
+    grid-template-columns: repeat(4, minmax(0, 1fr));
+    gap: var(--space-md);
+    align-items: end;
+}
+
+.compact-grid {
+    grid-template-columns: repeat(2, minmax(0, 1fr));
+}
+
+.inline-form,
+.inline-label {
+    display: flex;
+    align-items: center;
+    gap: var(--space-sm);
+}
+
+.inline-form {
+    width: 100%;
+}
+
+.inline-form select {
+    flex: 1 1 auto;
+}
+
+.business-select-form select {
+    min-width: 320px;
+}
+
 .appointment-create-actions {
     grid-column: 1 / -1;
     display: flex;
@@ -306,29 +451,126 @@ h2 {
     min-width: 180px;
 }
 
-.inline-form,
-.inline-label {
+.settings-grid {
+    display: grid;
+    grid-template-columns: repeat(2, minmax(0, 1fr));
+    align-items: stretch;
+    gap: var(--space-lg);
+}
+
+.settings-card {
+    margin-bottom: 0;
+}
+
+.settings-card.stack {
+    gap: var(--space-md);
+}
+
+.settings-actions {
+    display: flex;
+    justify-content: flex-end;
+    gap: var(--space-sm);
+}
+
+.settings-actions .button {
+    min-width: 180px;
+}
+
+.field-help {
+    color: var(--muted);
+    font-size: .8rem;
+    font-weight: 500;
+    line-height: 1.4;
+}
+
+.checkbox-grid {
+    display: grid;
+    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
+    gap: var(--space-sm);
+}
+
+.check-card {
+    display: flex;
+    align-items: flex-start;
+    gap: var(--space-sm);
+    min-width: 0;
+    padding: 12px;
+    border: 1px solid var(--border);
+    border-radius: var(--radius-md);
+    background: #fff;
+    cursor: pointer;
+    transition: border-color var(--transition), box-shadow var(--transition), background var(--transition);
+}
+
+.check-card:hover {
+    border-color: #cbd5e1;
+    background: #fafafa;
+}
+
+.check-card input {
+    flex: 0 0 auto;
+    margin-top: 3px;
+}
+
+.check-card span {
+    min-width: 0;
+}
+
+.check-card small {
+    display: block;
+    margin-top: 2px;
+    color: var(--muted);
+    font-weight: 500;
+    line-height: 1.35;
+}
+
+.availability-grid {
+    display: grid;
+    gap: var(--space-sm);
+}
+
+.availability-row {
+    display: grid;
+    grid-template-columns: minmax(180px, 1.5fr) minmax(120px, 1fr) minmax(120px, 1fr);
+    align-items: center;
+    gap: var(--space-sm);
+    padding: 10px 12px;
+    border: 1px solid var(--border);
+    border-radius: var(--radius-md);
+    background: #fff;
+}
+
+.availability-enabled {
     display: flex;
     align-items: center;
-    gap: 10px;
+    gap: 8px;
 }
 
+.availability-enabled input {
+    width: auto;
+}
+
+/* 8. Botões */
 .button {
-    border: 0;
-    border-radius: 12px;
-    background: var(--surface-2);
-    color: var(--text);
-    padding: 10px 14px;
-    cursor: pointer;
     display: inline-flex;
     align-items: center;
     justify-content: center;
     gap: 8px;
-    font-weight: 700;
+    min-height: 42px;
+    padding: 10px 14px;
+    border: 1px solid transparent;
+    border-radius: 12px;
+    background: var(--surface-2);
+    color: var(--text);
+    cursor: pointer;
+    font-weight: 750;
+    line-height: 1.15;
+    transition: background var(--transition), color var(--transition), border-color var(--transition), transform var(--transition), box-shadow var(--transition);
 }
 
 .button:hover {
-    filter: brightness(.97);
+    background: var(--secondary-hover);
+    transform: translateY(-1px);
 }
 
 .button.primary {
@@ -354,10 +596,14 @@ h2 {
     color: var(--config-text);
 }
 
+.button.config:hover {
+    background: #bfdbfe;
+}
+
 .button.cancel-edit {
+    border-color: var(--border);
     background: #f3f4f6;
     color: var(--muted);
-    border: 1px solid var(--border);
 }
 
 .button.cancel-edit:hover {
@@ -384,16 +630,36 @@ h2 {
 }
 
 .button.ghost {
+    width: 100%;
+    border-color: rgba(255, 255, 255, .15);
     background: transparent;
     color: #cbd5e1;
-    border: 1px solid rgba(255, 255, 255, .15);
-    width: 100%;
+}
+
+.button.ghost:hover {
+    border-color: rgba(255, 255, 255, .28);
+    background: rgba(255, 255, 255, .08);
+    color: white;
 }
 
 .button.small {
+    min-height: 34px;
     padding: 7px 10px;
+    border-radius: var(--radius-sm);
     font-size: .88rem;
-    border-radius: 10px;
+}
+
+/* 9. Tabelas */
+.table-shell {
+    width: 100%;
+    overflow-x: auto;
+    border: 1px solid var(--border);
+    border-radius: var(--radius-md);
+    background: var(--surface);
+}
+
+.table-shell .table {
+    margin: 0;
 }
 
 .table {
@@ -403,17 +669,27 @@ h2 {
 
 .table th,
 .table td {
-    text-align: left;
+    padding: 12px 10px;
     border-bottom: 1px solid var(--border);
-    padding: 12px 8px;
+    text-align: left;
     vertical-align: middle;
 }
 
 .table th {
+    background: #f9fafb;
     color: var(--muted);
     font-size: .82rem;
-    text-transform: uppercase;
+    font-weight: 800;
     letter-spacing: .06em;
+    text-transform: uppercase;
+}
+
+.table tbody tr:hover {
+    background: #fafafa;
+}
+
+.table tbody tr:last-child td {
+    border-bottom: 0;
 }
 
 .table .actions {
@@ -422,6 +698,14 @@ h2 {
     white-space: nowrap;
 }
 
+.table .actions form .button {
+    width: auto;
+}
+
+.table .detached {
+    border-bottom: 1px solid var(--border);
+}
+
 .admin-data-table {
     table-layout: fixed;
 }
@@ -436,6 +720,23 @@ h2 {
     min-width: 0;
 }
 
+.admin-data-table input[readonly],
+.admin-data-table select:disabled {
+    padding-right: 0;
+    padding-left: 0;
+    border-color: transparent;
+    background: transparent;
+}
+
+.admin-data-table tr.is-editing input,
+.admin-data-table tr.is-editing select {
+    padding-right: 12px;
+    padding-left: 12px;
+    border-color: var(--border);
+    background: #fff;
+    color: var(--text);
+}
+
 .admin-data-table .actions-buttons {
     display: flex;
     align-items: center;
@@ -459,10 +760,6 @@ h2 {
     margin: 0;
 }
 
-.table .actions form .button {
-    width: auto;
-}
-
 .clients-table th:nth-child(1),
 .clients-table td:nth-child(1) {
     width: 50px;
@@ -545,28 +842,42 @@ h2 {
     display: block;
 }
 
-.table .detached {
-    border-bottom: 1px solid var(--border);
+/* 10. Badges, notices e estados vazios */
+.notice {
+    margin-bottom: var(--space-lg);
+    padding: 12px 14px;
+    border: 1px solid #c7d2fe;
+    border-radius: var(--radius-md);
+    background: #eef2ff;
+    color: #3730a3;
+    line-height: 1.5;
 }
 
 .empty {
+    margin: 0;
+    padding: var(--space-lg);
+    border: 1px dashed #d1d5db;
+    border-radius: var(--radius-md);
+    background: #fafafa;
     color: var(--muted);
-    padding: 16px 0;
+    text-align: center;
 }
 
 .flash {
     position: fixed;
-    right: 24px;
-    bottom: 24px;
+    right: var(--space-lg);
+    bottom: var(--space-lg);
     z-index: 100;
     display: inline-flex;
     align-items: center;
     width: fit-content;
-    max-width: min(360px, calc(100vw - 32px));
+    max-width: min(420px, calc(100vw - 32px));
     padding: 14px 16px;
-    border-radius: 14px;
-    font-weight: 700;
-    box-shadow: var(--shadow);
+    border: 1px solid rgba(15, 23, 42, .08);
+    border-radius: var(--radius-md);
+    box-shadow: 0 18px 48px rgba(15, 23, 42, .16);
+    font-weight: 750;
+    line-height: 1.4;
     white-space: normal;
 }
 
@@ -582,11 +893,13 @@ h2 {
 
 .badge {
     display: inline-flex;
+    align-items: center;
     padding: 4px 9px;
     border-radius: 999px;
+    background: var(--surface-2);
     font-size: .78rem;
     font-weight: 800;
-    background: var(--surface-2);
+    line-height: 1.2;
 }
 
 .badge.scheduled {
@@ -604,58 +917,7 @@ h2 {
     color: #991b1b;
 }
 
-.checkbox-grid {
-    display: grid;
-    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
-    gap: 12px;
-}
-
-.check-card {
-    display: flex;
-    grid-template-columns: auto 1fr;
-    align-items: center;
-    gap: 10px;
-    border: 1px solid var(--border);
-    border-radius: 14px;
-    padding: 12px;
-}
-
-.check-card input {
-    width: auto;
-}
-
-.check-card small {
-    display: block;
-    color: var(--muted);
-    margin-top: 2px;
-}
-
-.availability-grid {
-    display: grid;
-    gap: 10px;
-}
-
-.availability-row {
-    display: grid;
-    grid-template-columns: 1.5fr 1fr 1fr;
-    gap: 10px;
-    align-items: center;
-}
-
-.availability-enabled {
-    display: flex;
-    align-items: center;
-    gap: 8px;
-}
-
-.availability-enabled input {
-    width: auto;
-}
-
-.fc .fc-toolbar-title {
-    font-size: 1.2rem;
-}
-
+/* 11. Modal */
 .modal[hidden] {
     display: none;
 }
@@ -672,7 +934,7 @@ h2 {
 .modal-backdrop {
     position: absolute;
     inset: 0;
-    background: rgba(15, 23, 42, .55);
+    background: rgba(15, 23, 42, .58);
 }
 
 .modal-card {
@@ -681,9 +943,10 @@ h2 {
     width: min(760px, 100%);
     max-height: 92vh;
     overflow: auto;
-    background: white;
+    padding: var(--space-lg);
+    border: 1px solid rgba(255, 255, 255, .45);
     border-radius: 22px;
-    padding: 24px;
+    background: white;
     box-shadow: 0 24px 80px rgba(0, 0, 0, .28);
 }
 
@@ -691,23 +954,32 @@ h2 {
     position: absolute;
     top: 14px;
     right: 14px;
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
     width: 34px;
     height: 34px;
-    border: 0;
+    border: 1px solid var(--border);
     border-radius: 999px;
+    background: #fff;
+    color: var(--muted);
     cursor: pointer;
     font-size: 1.4rem;
-    display: inline-flex;
-    align-items: center;
-    justify-content: center;
     line-height: 1;
+    transition: background var(--transition), color var(--transition), transform var(--transition);
+}
+
+.modal-close:hover {
+    background: #f3f4f6;
+    color: var(--text);
+    transform: rotate(4deg);
 }
 
 .modal-actions {
     display: grid;
     grid-template-columns: repeat(2, minmax(0, 1fr));
-    gap: 10px;
-    margin-top: 16px;
+    gap: var(--space-sm);
+    margin-top: var(--space-md);
 }
 
 .modal-actions form,
@@ -717,84 +989,108 @@ h2 {
 
 .modal-save-button {
     width: 100%;
-    margin-top: 10px;
+    margin-top: var(--space-sm);
 }
 
 .details-list {
-    background: var(--surface-2);
-    border-radius: 14px;
+    display: grid;
+    gap: 8px;
+    margin: var(--space-md) 0;
     padding: 12px 14px;
-    margin: 14px 0;
-}
-
-.compact-form {
-    margin-top: 14px;
+    border: 1px solid var(--border);
+    border-radius: var(--radius-md);
+    background: var(--surface-2);
 }
 
-pre {
-    white-space:
-        pre-wrap;
-    background: #111827;
-    color: #e5e7eb;
-    padding: 12px;
-    border-radius: 12px;
-    overflow: auto;
+.details-list p {
+    margin: 0;
 }
 
-.settings-grid {
-    display: grid;
-    grid-template-columns: repeat(2, minmax(0, 1fr));
-    gap: 20px;
-    align-items: start;
+.compact-form {
+    margin-top: var(--space-md);
 }
 
-.settings-card {
-    margin-bottom: 0;
+/* 12. Calendário */
+.calendar-card {
+    overflow: hidden;
 }
 
-.settings-actions {
-    display: flex;
-    justify-content: flex-end;
+#calendar {
+    min-width: 0;
 }
 
-.settings-actions .button {
-    min-width: 180px;
+.fc {
+    font-family: inherit;
 }
 
-.field-help {
-    color: var(--muted);
-    font-weight: 500;
-    font-size: .8rem;
-    line-height: 1.35;
+.fc .fc-toolbar-title {
+    font-size: 1.25rem;
+    font-weight: 800;
 }
 
-.section-title {
-    margin-bottom: 4px;
+.fc .fc-button {
+    border-radius: var(--radius-sm) !important;
+    font-weight: 700 !important;
 }
 
-.compact-grid {
-    grid-template-columns: repeat(2, minmax(0, 1fr));
+.fc .fc-event {
+    border-radius: 8px;
+    padding: 2px 4px;
 }
 
+/* 13. Integrações e configurações */
 .integration-title {
     display: flex;
     justify-content: space-between;
     align-items: flex-start;
-    gap: 12px;
-    margin-bottom: 12px;
+    gap: var(--space-md);
+    margin-bottom: var(--space-sm);
+}
+
+.integration-title h2 {
+    min-width: 0;
 }
 
 .integration-meta {
+    flex: 0 0 auto;
     color: var(--muted);
-    font-weight: 800;
     font-size: .82rem;
+    font-weight: 800;
+    line-height: 1.4;
     white-space: nowrap;
 }
 
+details {
+    margin-top: var(--space-md);
+    border: 1px solid var(--border);
+    border-radius: var(--radius-md);
+    background: #fafafa;
+}
+
+summary {
+    padding: 12px 14px;
+    cursor: pointer;
+    font-weight: 800;
+}
+
+details > .stack,
+details > pre {
+    margin: 0 var(--space-md) var(--space-md);
+}
+
+pre {
+    overflow: auto;
+    padding: 12px;
+    border-radius: 12px;
+    background: #111827;
+    color: #e5e7eb;
+    white-space: pre-wrap;
+}
+
+/* 14. Responsividade existente */
 @media (max-width: 980px) {
     .sidebar {
-        position:
-            static;
+        position: static;
         width: auto;
     }
 
@@ -815,13 +1111,23 @@ pre {
     .main {
         padding: 18px;
     }
-}
-@media (max-width: 980px) {
+
     .appointment-create-actions {
         justify-content: stretch;
     }
 
-    .appointment-create-button {
+    .appointment-create-button,
+    .inline-form .button {
         width: 100%;
     }
-}
\ No newline at end of file
+
+    .inline-form,
+    .inline-label {
+        align-items: stretch;
+        flex-direction: column;
+    }
+
+    .business-select-form select {
+        min-width: 0;
+    }
+}
diff --git a/src/templates/admin/account/password.html b/src/templates/admin/account/password.html
index f838d85..77cae74 100644
--- a/src/templates/admin/account/password.html
+++ b/src/templates/admin/account/password.html
@@ -10,7 +10,7 @@
     </div>
 </header>
 
-<section class="card settings-card">
+<section class="card settings-card narrow-card">
     <form method="post" action="/admin/account/password" class="stack">
         <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
         <label>Senha atual
diff --git a/src/templates/admin/appointments/calendar.html b/src/templates/admin/appointments/calendar.html
index 88db844..ca10d68 100644
--- a/src/templates/admin/appointments/calendar.html
+++ b/src/templates/admin/appointments/calendar.html
@@ -65,7 +65,7 @@
     </form>
 </section>
 
-<section class="card">
+<section class="card calendar-card">
     <div class="card-header">
         <h2>Agenda</h2>
         <label class="inline-label">Filtrar Profissional
diff --git a/src/templates/admin/businesses/index.html b/src/templates/admin/businesses/index.html
index ba726a4..5cd0d1d 100644
--- a/src/templates/admin/businesses/index.html
+++ b/src/templates/admin/businesses/index.html
@@ -1,13 +1,13 @@
 {% extends "admin/base.html" %}
 
-{% block title %}Selecionar empresa - BeautyflowAdmin{% endblock %}
+{% block title %}Selecionar empresa - Beautyflow Admin{% endblock %}
 
 {% block content %}
 <header class="page-header">
     <h1>Selecionar empresa</h1>
 </header>
 <section class="card">
-    <form method="post" action="/admin/businesses/select" class="inline-form">
+    <form method="post" action="/admin/businesses/select" class="inline-form business-select-form">
         <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
         <select name="business_id" required>
             {% for business in businesses %}
diff --git a/src/templates/admin/clients/index.html b/src/templates/admin/clients/index.html
index 61a2c83..bb54589 100644
--- a/src/templates/admin/clients/index.html
+++ b/src/templates/admin/clients/index.html
@@ -36,7 +36,8 @@
         <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
     </form>
     {% endfor %}
-    <table class="table admin-data-table clients-table">
+    <div class="table-shell">
+        <table class="table admin-data-table clients-table">
         <thead>
             <tr>
                 <th>ID</th>
@@ -76,7 +77,8 @@
             </tr>
             {% endfor %}
         </tbody>
-    </table>
+        </table>
+    </div>
     {% else %}
     <p class="empty">Nenhum cliente encontrado.</p>
     {% endif %}
diff --git a/src/templates/admin/dashboard.html b/src/templates/admin/dashboard.html
index b5f8c10..40677ef 100644
--- a/src/templates/admin/dashboard.html
+++ b/src/templates/admin/dashboard.html
@@ -31,7 +31,8 @@
         <a class="button small" href="/admin/appointments">Abrir calendário</a>
     </div>
     {% if today_appointments %}
-    <table class="table">
+    <div class="table-shell">
+        <table class="table">
         <thead>
             <tr>
                 <th>Horário</th>
@@ -57,7 +58,8 @@
             </tr>
             {% endfor %}
         </tbody>
-    </table>
+        </table>
+    </div>
     {% else %}
     <p class="empty">Nenhum agendamento para hoje.</p>
     {% endif %}
diff --git a/src/templates/admin/integrations/index.html b/src/templates/admin/integrations/index.html
index 9075482..030316c 100644
--- a/src/templates/admin/integrations/index.html
+++ b/src/templates/admin/integrations/index.html
@@ -1,6 +1,6 @@
 {% extends "admin/base.html" %}
 
-{% block title %}Integrações - BeautyflowAdmin{% endblock %}
+{% block title %}Integrações - Beautyflow Admin{% endblock %}
 
 {% block content %}
 <header class="page-header">
diff --git a/src/templates/admin/professionals/index.html b/src/templates/admin/professionals/index.html
index 6e908a2..9d5709d 100644
--- a/src/templates/admin/professionals/index.html
+++ b/src/templates/admin/professionals/index.html
@@ -1,6 +1,6 @@
 {% extends "admin/base.html" %}
 
-{% block title %}Profissionais - BeautyflowAdmin{% endblock %}
+{% block title %}Profissionais - Beautyflow Admin{% endblock %}
 
 {% block content %}
 <header class="page-header">
@@ -38,7 +38,8 @@
         <input type="hidden" name="_return_to" value="/admin/professionals" />
     </form>
     {% endfor %}
-    <table class="table admin-data-table professionals-table">
+    <div class="table-shell">
+        <table class="table admin-data-table professionals-table">
         <thead>
             <tr>
                 <th>ID</th>
@@ -89,7 +90,8 @@
             </tr>
             {% endfor %}
         </tbody>
-    </table>
+        </table>
+    </div>
     {% else %}
     <p class="empty">Nenhum profissional ativo.</p>
     {% endif %}
diff --git a/src/templates/admin/services/index.html b/src/templates/admin/services/index.html
index 1313866..52b7ed6 100644
--- a/src/templates/admin/services/index.html
+++ b/src/templates/admin/services/index.html
@@ -38,7 +38,8 @@
         <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
     </form>
     {% endfor %}
-    <table class="table admin-data-table services-table">
+    <div class="table-shell">
+        <table class="table admin-data-table services-table">
         <thead>
             <tr>
                 <th>ID</th>
@@ -88,7 +89,8 @@
             </tr>
             {% endfor %}
         </tbody>
-    </table>
+        </table>
+    </div>
     {% else %}
     <p class="empty">Nenhum serviço ativo.</p>
     {% endif %}
diff --git a/src/templates/admin/users/index.html b/src/templates/admin/users/index.html
index 7ecdb03..726f0cb 100644
--- a/src/templates/admin/users/index.html
+++ b/src/templates/admin/users/index.html
@@ -54,7 +54,8 @@
         <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
     </form>
     {% endfor %} {% endif %}
-    <table class="table admin-data-table users-table">
+    <div class="table-shell">
+        <table class="table admin-data-table users-table">
         <thead>
             <tr>
                 <th>ID</th>
@@ -119,7 +120,8 @@
             </tr>
             {% endfor %}
         </tbody>
-    </table>
+        </table>
+    </div>
     {% else %}
     <p class="empty">Nenhum usuário encontrado.</p>
     {% endif %}
